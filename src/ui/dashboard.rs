use ratatui::layout::Rect;
use ratatui::Frame;

use crate::app::App;

pub fn render_dashboard(f: &mut Frame, app: &App, area: Rect) {
    // Dashboard rendering is handled in mod.rs
    // This module can be extended for more complex dashboard layouts
}
